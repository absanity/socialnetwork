import { InvalidtypeDirective } from './invalidtype.directive';

describe('InvalidtypeDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidtypeDirective();
    expect(directive).toBeTruthy();
  });
});
